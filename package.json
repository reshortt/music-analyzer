{
  "name": "music-analyzer",
  "version": "1.0.0",
  "scripts": {
    "watch": "concurrently \"yarn webview:watch\" \"yarn extension:watch\" \"yarn server:dev\" \"yarn shared:watch\"",
    "webview:watch": "yarn --cwd webview watch",
    "extension:watch": "yarn --cwd extension watch",
    "server:dev": "yarn --cwd server dev",
    "shared:watch": "yarn --cwd shared watch"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
